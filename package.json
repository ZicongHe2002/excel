{
  "name": "sku-selection-workbench",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/build.mjs",
    "package:render": "node scripts/package-render.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.8.11",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "playwright": "^1.63.0",
    "vite": "^6.3.0"
  }
}
